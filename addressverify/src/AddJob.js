import React from 'react';

export default function AddJob(props){
  return (
    <div className={"collection"}>
      <button className={"button is-primary wide"}>Add New Job</button>
    </div>
  );
}
